

 var numberOptions = [10, 5, 3, 7];
// Next we create a for loop to create crystals for every numberOption.
//   for (var i = 0; i < numberOptions.length; i++) {
//     each.crystals.attr("data-crystalvalue", numberOptions[i]);
//     console.log(.crystals);
//   }

// pick a random number

//assign each crystal a random number from 1-12

//on click the value of crystal is added to a running total

//if the running total < random number, game continues. if it is > than random number display you lose
  
  
  
  //declare the array of possible number assignments to append to each crystal.

//   $.each(."crystal")function(){
//         var crystalNumber = 1 + Math.floor(Math.random() * 12);
//         console.log(crystalNumber);
//   }